import { StyleSheet } from "react-native";

export const indexStylesheets = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
        height: '100%',
        padding: 15
    },
    title: {
        fontSize: 27,
        fontWeight: 'bold'
    }
})